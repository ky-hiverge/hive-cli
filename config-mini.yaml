# The minimum configuration for Hive CLI.

project_name: beluga-direct-plan-project
repo:
  url: https://github.com/hiverge/beluga-direct-plan-project.git
sandbox:
  replicas: 2
  timeout: 60
wandb:
  enabled: false
# cloud vendor configuration
gcp:
  enabled: true
  project_id: runsandbox-449400
